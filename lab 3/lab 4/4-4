# ask user for speed
speed  = int(input("What is the speed of the vehicle in mph? "))

# ask for how many hours traveled
hours = int(input("Hpw many hours has it traveled? "))

#create loop for each hour
for hour in range (1, hours+1):
    distance = speed * hour
    print(f"{hour}\t{distance}")
    