def recursive_sum(lst):

    if not lst:
        return 0
    
    return lst[0] + recursive_sum(lst[1:])

#run program
numbers = [1, 56, 33, 17, 3]
result = recursive_sum(numbers)
print(f"The sum of the list is: {result}")
